# TASK
Clean up backend code: webapp\app.py

# SAMPLE QUERY
Fit for my use case

# SPECIAL INSTRUCTIONS
- DO NOT write complex custom code.
- Here is a sample query fit for my use case: 

SELECT e.id, v.distance
FROM images AS e
JOIN vector_quantize_scan('images', 'embedding', ?, 20) AS v
ON e.id = v.rowid;